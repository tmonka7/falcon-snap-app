*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build
# whisper.cpp sources (cloned locally, not committed)
app/src/main/cpp/whisper.cpp/

# speech models (large binaries: pushed to the device, or dropped in app/src/main/assets)
*.bin

# Models are not committed (NLLB is ~1 GB and lives on device storage, see MODELS.md) ...
/models/
*.onnx
*.onnx_data
# ... except the small OCR models bundled in the APK: without them a fresh clone has no OCR.
!/app/src/main/assets/*.onnx

# editor backup files
*~
