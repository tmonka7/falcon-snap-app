<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/lang_bar"
    android:layout_width="wrap_content"
    android:layout_height="@dimen/language_pill_height"
    android:background="@drawable/bg_pill_white"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="6dp"
    android:paddingEnd="6dp">

    <TextView
        android:id="@+id/lang_source"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:gravity="center"
        android:maxLines="1"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/language_pill_text_size" />

    <ImageView
        android:id="@+id/lang_swap"
        android:layout_width="@dimen/language_pill_swap_size"
        android:layout_height="@dimen/language_pill_swap_size"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/cd_swap"
        android:padding="4dp"
        android:src="@drawable/ic_swap"
        app:tint="@color/brand_red" />

    <TextView
        android:id="@+id/lang_target"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:gravity="center"
        android:maxLines="1"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/language_pill_text_size" />
</LinearLayout>
